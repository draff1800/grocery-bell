import React from 'react';
import {Text, View} from 'react-native';

const App = () => {
  return (
    // eslint-disable-next-line react-native/no-inline-styles
    <View style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
      <Text>Grocery Bell</Text>
    </View>
  );
};

export default App;
